{{define "file"}}
    <div class="form mb-3">
        {{template "label" .}}
        <input id="{{.Control.Name}}"
               data-testid="test-file"
               name="{{.Control.Name}}"
               value="{{if .Value}}{{index .Value 0}}{{end}}"
               class="form-control"
               type="file"
                {{/* TODO implement submiting files with forms. Keep this element disabled until then (it requires special care) */}}
               disabled
               placeholder="{{.Control.Placeholder.ForLocale "en"}}"
                {{if .Control.Validation.Required}}required{{end}}
               aria-describedby="{{.Control.Name}}Feedback"
                {{if .Control.Readonly}}disabled readonly{{end}}
        >
        {{template "description" .}}
        {{template "fc_feedback" .}}
    </div>
{{end}}
