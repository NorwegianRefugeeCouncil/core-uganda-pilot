{
  "name": "web",
  "license": "Apache-2.0",
  "packageManager": "yarn@3.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "libs/*"
  ],
  "devDependencies": {
    "esbuild": "^0.13.15",
    "ts-node": "^10.4.0",
    "typescript": "^4.5.2"
  },
  "scripts": {
    "build": "ts-node ./scripts/clean.ts && ts-node ./scripts/build.ts",
    "pwa:serve": "yarn workspace @core/pwa start",
    "pwa:test": "yarn workspace @core/pwa start",
    "intake-app:serve": "yarn workspace @core/intake-app start",
    "intake-app:test": "yarn workspace @core/intake-app test",
    "api-client:test": "yarn workspace @core/api-client test",
    "auth-client:test": "yarn workspace @core/auth-client test",
    "storybook:serve": "yarn workspace @core/storybook storybook",
    "admin:serve": "yarn workspace @core/admin start",
    "admin:test": "yarn workspace @core/admin test"
  },
  "resolutions": {
    "ua-parser-js": "^0.7.30"
  }
}
