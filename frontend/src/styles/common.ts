import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  top: {
    position: 'absolute',
    left: 0,
    right: 0,
    top: 0,
  },
  logo: {
    height: 40,
    width: 40,
  }
});
