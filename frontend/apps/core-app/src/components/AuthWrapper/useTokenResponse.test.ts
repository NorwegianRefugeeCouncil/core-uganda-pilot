import { useTokenResponse } from './useTokenResponse';

describe('Success', () => {
  it('stub', () => {
    expect(true).toBe(true);
  });
});

describe('Failure', () => {
  it('stub', () => {
    expect(true).toBe(true);
  });
});
