package entity

import (
	"context"

	"github.com/nrc-no/core/pkg/server/core-db/types"
)

func (d *entityService) Get(ctx context.Context, entityID string) (*types.Entity, error) {
	return nil, nil
}
